{
  "name": "@roomhash/voting",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "cargo test",
    "check": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && npm run build && node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
